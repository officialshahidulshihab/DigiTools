import React from 'react';

const PricingFeatures = ({item}) => {
    return (
        <div>
            <ul>
                <li>{item.text}</li>
            </ul>
        </div>
    );
};

export default PricingFeatures;